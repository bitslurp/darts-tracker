import React, {FunctionComponent} from 'react';
import {SafeAreaView, Text} from 'react-native';

const App: FunctionComponent = () => {
  return (
    <SafeAreaView style={{backgroundColor: 'white'}}>
      <Text>Darts TRACKER</Text>
    </SafeAreaView>
  );
};

export default App;
